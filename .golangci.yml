version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - gosec
    - ineffassign
    - errorlint
    - noctx
  settings:
    gosec:
      severity: medium
  exclusions:
    presets:
      - common-false-positives

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/ixofoundation/vault-argocd-watcher
